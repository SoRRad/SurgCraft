import { getBundleById, getBundleOpportunities } from '@/lib/opportunities/bundles';export default function Page({params}:{params:{bundleId:string}}){const b=getBundleById(params.bundleId);const o=getBundleOpportunities(params.bundleId);if(!b)return <main className='p-6'>Needs more curated opportunities.</main>;return <main className='p-6'><h1>{b.title}</h1><p>{b.description}</p><p>{o.length} opportunities</p></main>}
