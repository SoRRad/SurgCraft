"use client"
import { useMemo, useState } from "react"
import { ChatLayout } from "@/components/chat/ChatLayout"
import { SectionMarker } from "@/components/shell/SectionMarker"
import { Button } from "@/components/ui/button"
const BANK: Record<string, { q: string; best: string; miss: string; follow: string; hook: string }[]> = {"Fight bite":[{q:"What is first-line IV antibiotic coverage?",best:"Ampicillin-sulbactam with urgent washout eval.",miss:"Oral cephalexin misses Eikenella coverage.",follow:"When do you take to OR?",hook:"MCP bite = joint until proven otherwise."}],"Mallet finger":[{q:"How should a mallet splint be applied?",best:"DIP in extension continuously; PIP free.",miss:"Any DIP flexion resets treatment clock.",follow:"When do you consider surgery?",hook:"DIP only; PIP party stays open."}]}
export default function RapidPage(){const topics=["Fight bite","Mallet finger","Distal radius fracture","Flexor tendon zones","Kanavel signs","Scaphoid fracture","Extensor compartments"];const [topic,setTopic]=useState(topics[0]);const [intensity,setIntensity]=useState("Standard");const [answer,setAnswer]=useState("");const [submitted,setSubmitted]=useState(false);const item=useMemo(()=>BANK[topic]?.[0]??{q:`Give a high-yield one-liner for ${topic}.`,best:`${topic}: summarize diagnosis and first management step.`,miss:"Be explicit about red flags.",follow:"What can be missed on first pass?",hook:"Name diagnosis + first move."},[topic]);const score=answer.trim().length<20?0:answer.toLowerCase().includes("urgent")?3:2;const labels=["Needs review","Partially correct","Good","Rounds-ready"];return <ChatLayout><div className="flex-1 overflow-y-auto"><div className="mx-auto max-w-screen-xl px-4 py-8 sm:px-6 lg:px-8"><SectionMarker number="04" label="Rapid Q&A" className="mb-3" /><h1 className="text-h1 font-fraunces text-ink">Rapid Q&A <span className="text-small text-ink-muted">(Pimping-style practice, without scores being shared)</span></h1><div className="mt-4 grid gap-3 md:grid-cols-2"><select value={topic} onChange={(e)=>{setTopic(e.target.value);setSubmitted(false);setAnswer("")}} className="rounded-xl bg-bg-elevated px-3 py-2 ring-1 ring-rule/70">{topics.map(t=><option key={t}>{t}</option>)}</select><select value={intensity} onChange={(e)=>setIntensity(e.target.value)} className="rounded-xl bg-bg-elevated px-3 py-2 ring-1 ring-rule/70">{["Gentle","Standard","Oral boards"].map(t=><option key={t}>{t}</option>)}</select></div><div className="mt-4 rounded-2xl bg-bg-elevated p-4 ring-1 ring-rule/70 shadow-soft"><p className="text-micro uppercase tracking-wide text-ink-faint">{intensity}</p><p className="mt-1 text-body text-ink">{item.q}</p><textarea value={answer} onChange={(e)=>setAnswer(e.target.value)} className="mt-3 min-h-24 w-full rounded-xl bg-bg px-3 py-2 ring-1 ring-rule/70" placeholder="Type your answer..."/><div className="mt-3 flex flex-wrap gap-2"><Button onClick={()=>setSubmitted(true)}>Check answer</Button><Button variant="outline" onClick={()=>{setSubmitted(false);setAnswer("")}}>Next question</Button><Button variant="ghost" onClick={()=>{setSubmitted(false);setAnswer("")}}>End session</Button><Button asChild variant="outline"><a href="/c">Back to chat</a></Button></div>{submitted&&<div className="mt-4 space-y-2 text-small"><p><strong>Feedback:</strong> {labels[score]}</p><p><strong>Best answer:</strong> {item.best}</p><p><strong>Missing key points:</strong> {item.miss}</p><p><strong>Attending follow-up:</strong> {item.follow}</p><p><strong>Memory hook:</strong> {item.hook}</p></div>}</div></div></div></ChatLayout>}
