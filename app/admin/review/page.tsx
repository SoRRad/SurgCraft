import { OPPORTUNITIES } from '@/lib/opportunities/data'
export default function Page(){const total=OPPORTUNITIES.length;const conf=OPPORTUNITIES.filter(o=>['conference','meeting'].includes(o.type)).length;const funding=OPPORTUNITIES.filter(o=>['grant','funding','award'].includes(o.type)).length;const travel=OPPORTUNITIES.filter(o=>['travel-scholarship','resident-competition'].includes(o.type)).length;const needs=OPPORTUNITIES.filter(o=>o.reviewStatus==='needs-review').length;return <main className='p-6 space-y-2'><h1 className='text-2xl'>Content review</h1><p>Opportunity Hub records: {total}</p><p>Conferences/meetings: {conf}</p><p>Grants/funding/awards: {funding}</p><p>Travel scholarships/resident opportunities: {travel}</p><p>Needs review: {needs}</p><p className='text-sm text-amber-700'>Deadlines, eligibility, and funding amounts must be verified on official websites before submission.</p></main>}
