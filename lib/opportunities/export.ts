import type { Opportunity } from './types'
export const opportunitiesToCsv=(ops:Opportunity[])=>['id,title,type,organization,deadlineDate,startDate,officialUrl',...ops.map(o=>[o.id,o.title,o.type,o.organization,o.deadlineDate||'',o.startDate||'',o.officialUrl].map(v=>`"${String(v).replaceAll('"','""')}"`).join(','))].join('\n')
export const opportunityDeadlinesToIcs=(ops:Opportunity[])=>{const lines=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//ORION//Opportunity Hub//EN'];for(const o of ops){for(const dt of [o.deadlineDate,o.startDate]){if(!dt)continue;const d=new Date(dt);if(Number.isNaN(d.getTime()))continue;const y=d.toISOString().slice(0,10).replaceAll('-','');lines.push('BEGIN:VEVENT',`UID:${o.id}-${y}@orion`,`DTSTAMP:${y}T000000Z`,`DTSTART;VALUE=DATE:${y}`,`SUMMARY:${o.title}`,`DESCRIPTION:${o.description} ${o.officialUrl}`,`LOCATION:${o.city||o.country||'Virtual'}`,'END:VEVENT')}}lines.push('END:VCALENDAR');return lines.join('\n')}
export const downloadTextFile=(filename:string,content:string,mimeType:string)=>{if(typeof window==='undefined')return;const blob=new Blob([content],{type:mimeType});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;a.click();URL.revokeObjectURL(url)}
