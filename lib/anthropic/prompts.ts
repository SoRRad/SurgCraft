// Week 2: system prompts per mode, loaded from prompts/ folder
// Each prompt is loaded at runtime so faculty can edit without a deploy
export type Mode = "tutor" | "case" | "pimping" | "preop" | "debrief" | "consult"
